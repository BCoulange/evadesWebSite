<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :prenom %><br />
    <%= f.text_field :prenom %>
  </div>
  <div class="field">
    <%= f.label :nom %><br />
    <%= f.text_field :nom %>
  </div>
  <div class="field">
    <%= f.label :surnom %><br />
    <%= f.text_field :surnom %>
  </div>

  <p>Spectacles :</p><br />
  <%= collection_select(:user, :spectacle_ids, @spectacles, :id, :titre, {:prompt => " "}, { :multiple => true })%>
  <p>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
